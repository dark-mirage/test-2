# Как добавить товары на бекенд

## 🎯 Быстрый способ через Swagger UI

### Шаг 1: Откройте Swagger
```
https://loyaltymarket.ru:7890/docs
```

### Шаг 2: Создайте категорию
1. Найдите `POST /api/v1/categories`
2. Нажмите "Try it out"
3. Введите:
```json
{
  "name": "Одежда"
}
```
4. Нажмите "Execute"
5. **Запомните ID** (например: `1`)

### Шаг 3: Создайте тип
1. Найдите `POST /api/v1/types`
2. Введите:
```json
{
  "name": "Футболки",
  "category_id": 1
}
```
3. **Запомните ID** типа

### Шаг 4: Создайте бренд
1. Найдите `POST /api/v1/brands`
2. Введите:
```json
{
  "name": "Supreme"
}
```
3. **Запомните ID** бренда

### Шаг 5: Создайте товар
1. Найдите `POST /api/v1/products/`
2. **Важно:** Это `multipart/form-data`, не JSON!
3. Заполните форму:
   - `name`: Кофта Supreme
   - `price`: 127899
   - `category_id`: 1
   - `type_id`: 1
   - `brand_id`: 1
   - `delivery`: China (или Orenburg, или Moscow)
   - `photos`: [выберите файл изображения]
4. Нажмите "Execute"

---

## 🚀 Автоматический способ (через консоль браузера)

### Вариант 1: Использовать готовый скрипт

1. Откройте приложение в браузере
2. Откройте консоль (F12)
3. Выполните:

```javascript
// Импортируем API
const api = await import('/lib/api.js');

// Создать категорию
const category = await api.categoriesApi.create({ name: 'Одежда' });
console.log('Категория:', category);

// Создать тип
const type = await api.typesApi.create({ 
  name: 'Футболки', 
  category_id: category.id 
});
console.log('Тип:', type);

// Создать бренд
const brand = await api.brandsApi.create({ name: 'Supreme' });
console.log('Бренд:', brand);
```

### Вариант 2: Использовать функцию seedData

```javascript
// Импортируем скрипт
await import('/scripts/seedDataBrowser.js');

// Запустить создание данных
await seedData();
```

---

## 📝 Пример создания товара через консоль

**Внимание:** Товары требуют загрузки фото, поэтому проще через Swagger UI.

Но можно попробовать через консоль (если есть URL изображения):

```javascript
const api = await import('/lib/api.js');

// Сначала нужно получить File объект изображения
// Это сложно в консоли, поэтому лучше через Swagger

// Но можно создать товар без фото (если API позволяет):
const product = await api.productsApi.create({
  name: 'Тестовый товар',
  price: 10000,
  category_id: 1,
  type_id: 1,
  brand_id: 1,
  delivery: 'China',
  photos: [] // Пустой массив, если фото не обязательны
});
```

---

## ✅ Проверка, что данные добавлены

После создания данных проверьте:

```javascript
// В консоли браузера
testAPI()
```

Или откройте страницы:
- Главная (`/`) - должны появиться товары
- Каталог (`/catalog`) - должны быть категории
- Бренды - должны быть бренды

---

## 🎨 Рекомендация

**Самый простой способ:**
1. Используйте **Swagger UI** для создания товаров
2. Это удобно для загрузки фото
3. Видно все поля и их типы
4. Можно сразу проверить результат

**Для массового создания:**
- Используйте скрипт `seedDataBrowser.js` для категорий, типов, брендов
- Товары создавайте через Swagger (нужны фото)

---

## 📋 Чек-лист создания данных

- [ ] Создать категории (минимум 1)
- [ ] Создать типы (минимум 1)
- [ ] Создать бренды (минимум 1)
- [ ] Создать товары (минимум 3-5 для тестирования)
- [ ] Проверить, что товары отображаются на главной странице
- [ ] Проверить, что можно открыть страницу товара

После этого фронтенд будет работать с реальными данными!

