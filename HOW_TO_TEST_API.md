# Как проверить, что API работает

## 🧪 Способ 1: Тестовая функция в консоли браузера

1. Откройте приложение в браузере
2. Откройте консоль разработчика (F12 или Cmd+Option+I)
3. В консоли выполните:

```javascript
testAPI()
```

Эта функция автоматически протестирует все основные эндпоинты API и покажет результаты.

## 🔍 Способ 2: Проверка в консоли браузера

Откройте консоль разработчика (F12) и проверьте:

### 1. Проверка загрузки данных на главной странице

Откройте главную страницу и в консоли должны быть логи:
- `Failed to load products:` - если есть ошибки
- Или данные должны загрузиться без ошибок

### 2. Проверка Network запросов

1. Откройте вкладку **Network** в DevTools
2. Обновите страницу (F5)
3. Найдите запросы к `loyaltymarket.ru:7890`
4. Проверьте статус ответов:
   - **200** - успешно ✅
   - **401/403** - проблема с авторизацией
   - **404** - эндпоинт не найден
   - **500** - ошибка сервера

### 3. Проверка конкретных страниц

#### Главная страница (`/`)
- Должны загружаться товары с сервера
- В консоли не должно быть ошибок загрузки

#### Страница товара (`/product/[id]`)
- Откройте любой товар
- Должны загрузиться:
  - Данные товара
  - Фотографии
  - Размеры
  - Похожие товары

#### Корзина (`/trash`)
- Должна загружаться корзина с сервера
- Если корзина пустая - это нормально (если вы не добавляли товары)

#### Заказы (`/profile/orders`)
- Должны загружаться заказы пользователя
- Если заказов нет - это нормально

#### Избранное (`/favorites`)
- Должны загружаться избранные товары и бренды

## 🐛 Что проверить, если что-то не работает

### 1. Проверьте переменную окружения

Убедитесь, что файл `.env.local` существует и содержит:

```bash
NEXT_PUBLIC_API_BASE_URL=https://loyaltymarket.ru:7890
```

### 2. Проверьте авторизацию

В консоли выполните:

```javascript
// Проверить токен
document.cookie.match(/tg_session=([^;]+)/)?.[1]
```

Если токена нет - нужно авторизоваться через Telegram.

### 3. Проверьте CORS

Если видите ошибки CORS в консоли - нужно настроить CORS на бекенде.

### 4. Проверьте URL API

В консоли выполните:

```javascript
// Проверить базовый URL
process.env.NEXT_PUBLIC_API_BASE_URL
```

Должно показать: `https://loyaltymarket.ru:7890`

## ✅ Признаки, что API работает:

1. ✅ На главной странице загружаются товары с сервера
2. ✅ При открытии товара загружаются его данные
3. ✅ Можно добавить товар в корзину (и он сохраняется на сервере)
4. ✅ Можно добавить товар в избранное
5. ✅ В Network видны запросы к `loyaltymarket.ru:7890` со статусом 200
6. ✅ В консоли нет ошибок загрузки данных

## ❌ Признаки, что API НЕ работает:

1. ❌ На страницах показываются только моковые данные
2. ❌ В Network нет запросов к API
3. ❌ В консоли ошибки типа "Failed to load..."
4. ❌ Страницы показывают "Загрузка..." бесконечно
5. ❌ Ошибки 401/403 (проблема с авторизацией)
6. ❌ Ошибки 404 (неправильный URL эндпоинта)
7. ❌ Ошибки CORS

## 🔧 Быстрая проверка в консоли

Выполните в консоли браузера:

```javascript
// Проверить, что API клиент доступен
import('@/lib/api').then(api => {
  console.log('API клиент загружен:', api);
  
  // Попробовать получить товары
  api.productsApi.getLatest(3).then(products => {
    console.log('✅ API работает! Товары:', products);
  }).catch(err => {
    console.error('❌ Ошибка API:', err);
  });
});
```

## 📝 Логи для отладки

Все API запросы логируются в консоль. Если что-то не работает:

1. Откройте консоль
2. Найдите сообщения с `Failed to load...` или `API request failed:`
3. Проверьте детали ошибки
4. Убедитесь, что бекенд доступен и работает

