# 🚀 Быстрая проверка работы API

## Самый простой способ:

1. **Откройте приложение в браузере**
2. **Откройте консоль** (F12 или Cmd+Option+I)
3. **Выполните команду:**

```javascript
testAPI()
```

Вы увидите результаты тестирования всех API эндпоинтов!

---

## Визуальная проверка:

### ✅ Главная страница (`/`)
- Должны загружаться **реальные товары** с сервера
- Если видите "Загрузка..." и потом товары появляются - **API работает!**

### ✅ Страница товара (`/product/1` или любой ID)
- Откройте любой товар
- Должны загрузиться:
  - Название, цена, фото товара
  - Размеры
  - Похожие товары
- Если данные загружаются - **API работает!**

### ✅ Корзина (`/trash`)
- Если корзина пустая - это нормально (если не добавляли товары)
- Если есть товары - они должны загружаться с сервера
- **API работает!**

### ✅ Заказы (`/profile/orders`)
- Должны загружаться заказы (если они есть)
- Если заказов нет - это нормально
- **API работает!**

---

## Проверка в Network (DevTools):

1. Откройте **Network** вкладку (F12 → Network)
2. Обновите страницу (F5)
3. Найдите запросы к `loyaltymarket.ru:7890`
4. Если видите запросы со статусом **200** - **API работает!** ✅

---

## ❌ Если не работает:

1. Проверьте файл `.env.local` - должен содержать:
   ```
   NEXT_PUBLIC_API_BASE_URL=https://loyaltymarket.ru:7890
   ```

2. Перезапустите dev сервер:
   ```bash
   npm run dev
   ```

3. Проверьте консоль на ошибки

4. Убедитесь, что бекенд доступен по адресу `https://loyaltymarket.ru:7890`

