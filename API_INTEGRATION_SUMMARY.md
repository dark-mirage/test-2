# Сводка интеграции API

## ✅ Что было сделано:

### 1. Создан API клиент (`lib/api.js`)
- Полная интеграция всех эндпоинтов из Swagger
- Поддержка авторизации через Bearer токен
- Обработка ошибок
- Функции для всех модулей:
  - Users (пользователи)
  - Categories (категории)
  - Types (типы)
  - Brands (бренды)
  - Products (товары)
  - Favorites (избранное)
  - Cart (корзина)
  - Orders (заказы)
  - Payments (платежи)
  - Shipments (отправления)
  - PVZ (пункты выдачи)
  - Referrals (рефералы)

### 2. Созданы хуки для работы с API
- `lib/hooks/useApiCart.js` - хук для работы с корзиной через API
- `lib/hooks/useCurrentUser.js` - хук для получения текущего пользователя

### 3. Интегрированы компоненты:

#### ✅ Главная страница (`app/page.jsx`)
- Загрузка последних товаров через `productsApi.getLatest()`
- Загрузка рекомендуемых товаров
- Работа с избранным через API
- Отображение состояния загрузки

#### ✅ Страница товара (`app/product/[id]/page.jsx`)
- Загрузка данных товара через `productsApi.getById()`
- Загрузка фотографий товара
- Загрузка размеров
- Добавление в корзину через API
- Добавление/удаление из избранного
- Загрузка похожих товаров
- Загрузка брендов для карусели

#### ✅ Страница заказов (`app/profile/orders/OrdersClient.jsx`)
- Загрузка всех заказов пользователя через `ordersApi.getAll()`
- Преобразование данных заказов в формат компонента
- Загрузка товаров для каждого заказа
- Отображение статусов заказов

#### ✅ Страница избранного (`app/favorites/page.jsx`)
- Загрузка избранных товаров через `favoritesApi.getAll()`
- Загрузка избранных брендов
- Добавление/удаление из избранного
- Загрузка рекомендуемых товаров

### 4. Настройка окружения

**Важно:** Создайте файл `.env.local` в корне проекта:

```bash
NEXT_PUBLIC_API_BASE_URL=https://loyaltymarket.ru:7890
```

## ⚠️ Что нужно доработать:

### 1. Корзина (`app/trash/page.jsx`)
- Нужно заменить `useCart` на `useApiCart` для работы с API
- Обновить логику добавления/удаления товаров

### 2. Оформление заказа (`app/checkout/page.jsx`)
- Интегрировать создание заказа через `ordersApi.create()`
- Добавить работу с платежами через `paymentsApi`
- Интегрировать работу с отправлениями через `shipmentsApi`

### 3. Поиск (`app/search/page.jsx`)
- Заменить моки на реальные запросы к API
- Использовать `productsApi.getAll()` с фильтрами
- Интегрировать поиск брендов через `brandsApi.search()`

### 4. Каталог (`app/catalog/page.jsx`)
- Загружать категории через `categoriesApi.getAll()`
- Загружать типы через `typesApi.getAll()`
- Загружать бренды через `brandsApi.getAll()`

### 5. Рефералы (`app/invite-friends/page.jsx`)
- Интегрировать `referralsApi.getLink()`
- Интегрировать `referralsApi.getInvited()`
- Интегрировать `referralsApi.getDiscount()`
- Интегрировать `referralsApi.getStats()`

### 6. Получение user_id
- Сейчас используется хардкод `user_id: 1` в некоторых местах
- Нужно использовать `useCurrentUser()` хук везде, где нужен user_id
- Проверить, что токен авторизации правильно передается в запросах

## 📝 Примечания:

1. **Авторизация**: Токен берется из cookie `tg_session` автоматически
2. **Обработка ошибок**: Все ошибки логируются в консоль, но можно добавить toast-уведомления
3. **Загрузка изображений**: Используются прямые URL к API для получения изображений
4. **Состояния загрузки**: Добавлены индикаторы загрузки в основных компонентах

## 🚀 Следующие шаги:

1. Протестировать все интегрированные компоненты
2. Доработать оставшиеся страницы (корзина, checkout, поиск, каталог)
3. Добавить обработку ошибок с пользовательскими сообщениями
4. Оптимизировать загрузку данных (кэширование, prefetching)
5. Добавить индикаторы загрузки везде, где это необходимо

