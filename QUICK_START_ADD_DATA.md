# 🚀 Быстрое добавление товаров на бекенд

## ✅ Хорошие новости!

**API работает!** Из теста видно:
- ✅ Товары загружаются (получено 1 товар)
- ✅ Категории загружаются (получено 2 категории)  
- ✅ Бренды загружаются (получено 1 бренд)

Просто данных мало. Нужно добавить больше!

---

## 🎯 Самый простой способ (5 минут)

### Через Swagger UI:

1. **Откройте:** https://loyaltymarket.ru:7890/docs

2. **Создайте товар:**
   - Найдите `POST /api/v1/products/`
   - Нажмите "Try it out"
   - Заполните:
     ```
     name: Кофта Supreme
     price: 127899
     category_id: 1 (используйте существующую)
     type_id: 1 (используйте существующую)
     brand_id: 1 (используйте существующую)
     delivery: China
     photos: [выберите файл изображения]
     ```
   - Нажмите "Execute"

3. **Повторите** для других товаров

4. **Проверьте:** Обновите главную страницу - товары должны появиться!

---

## 🛠️ Через консоль браузера (для категорий/типов/брендов)

1. Откройте консоль (F12)
2. Выполните:

```javascript
// Импортируем API
const api = await import('/lib/api.js');

// Создать категорию
const cat = await api.categoriesApi.create({ name: 'Одежда' });
console.log('Категория создана:', cat.id);

// Создать тип
const type = await api.typesApi.create({ 
  name: 'Футболки', 
  category_id: cat.id 
});

// Создать бренд
const brand = await api.brandsApi.create({ name: 'Nike' });
```

---

## 📊 Текущее состояние

По результатам теста:
- **Категорий:** 2 ✅
- **Брендов:** 1 ✅
- **Товаров:** 1 ⚠️ (мало!)

**Что делать:**
1. Добавьте больше товаров через Swagger
2. Или создайте больше категорий/брендов через консоль
3. После этого фронтенд будет показывать реальные данные

---

## 💡 Совет

**Для товаров используйте Swagger** - там удобно загружать фото.

**Для категорий/типов/брендов** можно использовать консоль - быстрее.

После добавления данных просто обновите страницу - всё заработает!

