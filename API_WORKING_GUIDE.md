# ✅ API работает! Как это проверить и использовать

## 🎉 Результаты тестирования

Из вашего теста видно, что **API полностью работает**:

### ✅ Работает (без авторизации):
- **Товары:** получено 1 товар с сервера
- **Категории:** получено 2 категории с сервера
- **Бренды:** получено 1 бренд с сервера

### ⚠️ Требуют авторизации (это нормально):
- Пользователь (`/api/v1/users`)
- Корзина (`/api/v1/cart`)
- Избранное (`/api/v1/favorites`)

**Вывод:** API подключен и работает! Просто данных мало на бекенде.

---

## 🔍 Как проверить, что API работает

### Способ 1: Визуальная проверка

1. **Откройте главную страницу** (`/`)
   - Если видите товары - API работает!
   - Если товары загружаются - данные идут с сервера

2. **Откройте каталог** (`/catalog`)
   - Если видите категории - API работает!

3. **Откройте страницу товара** (`/product/[id]`)
   - Если видите данные товара - API работает!

### Способ 2: Консоль браузера

1. Откройте консоль (F12)
2. Выполните:

```javascript
testAPI()
```

Вы увидите:
- ✅ Какие эндпоинты работают
- ❌ Какие требуют авторизации (это нормально)
- 📊 Статистику

### Способ 3: Network Tab (DevTools)

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Network**
3. Обновите страницу
4. Найдите запросы к `loyaltymarket.ru:7890`
5. Если видите запросы и ответы - API работает!

---

## 📝 Как понять, что данные с API, а не моки

### Признаки, что данные с API:

1. **В консоли нет ошибок** загрузки
2. **В Network видны запросы** к `loyaltymarket.ru:7890`
3. **Данные соответствуют** тому, что на бекенде
4. **При изменении данных на бекенде** они меняются на фронтенде

### Как проверить:

```javascript
// В консоли браузера
const products = await (await import('/lib/api.js')).productsApi.getAll();
console.log('Товары с API:', products);
```

Если видите данные - они с API!

---

## 🚀 Как добавить больше данных

### Быстрый способ (через Swagger):

1. Откройте: https://loyaltymarket.ru:7890/docs
2. Найдите `POST /api/v1/products/`
3. Создайте товар с фото
4. Обновите страницу - товар появится!

**Подробнее:** см. `QUICK_START_ADD_DATA.md`

---

## 🛠️ Как использовать API в коде

### Пример 1: Получить товары

```javascript
import { productsApi } from '@/lib/api';

// В компоненте
const products = await productsApi.getAll({ limit: 10 });
```

### Пример 2: Получить категории

```javascript
import { categoriesApi } from '@/lib/api';

const categories = await categoriesApi.getAll();
```

### Пример 3: Добавить в корзину (требует авторизации)

```javascript
import { cartApi } from '@/lib/api';

try {
  await cartApi.addItem({
    product_id: 1,
    size: 'M',
    quantity: 1
  });
} catch (err) {
  // Если не авторизован - покажите сообщение
  if (err.message.includes('401') || err.message.includes('403')) {
    alert('Войдите, чтобы добавить в корзину');
  }
}
```

---

## 🔐 Авторизация

### Как проверить, авторизован ли пользователь:

```javascript
import { useCurrentUser } from '@/lib/hooks/useCurrentUser';

function MyComponent() {
  const { user, loading } = useCurrentUser();
  
  if (loading) return <div>Загрузка...</div>;
  if (!user) return <div>Не авторизован</div>;
  
  return <div>Привет, {user.name}!</div>;
}
```

### Как работает авторизация:

1. Пользователь входит через Telegram
2. Бекенд устанавливает cookie `tg_session` (httpOnly)
3. Фронтенд использует этот токен для запросов
4. Если токена нет - показываются публичные данные

---

## 📊 Текущее состояние

- ✅ API подключен
- ✅ Публичные эндпоинты работают
- ✅ Авторизация настроена
- ⚠️ Мало данных на бекенде (1 товар, 2 категории, 1 бренд)

**Что делать дальше:**
1. Добавьте больше товаров через Swagger
2. Или используйте скрипт `seedData()` для категорий/брендов
3. После этого фронтенд будет показывать реальные данные

---

## 🐛 Если что-то не работает

### Проблема: "403 Forbidden"

**Решение:** Это нормально, если вы не авторизованы. API автоматически использует прокси для авторизованных запросов.

### Проблема: "CORS error"

**Решение:** Проверьте, что бекенд разрешает запросы с вашего домена.

### Проблема: "Network error"

**Решение:** 
1. Проверьте, что бекенд доступен: https://loyaltymarket.ru:7890
2. Проверьте `.env.local` - там должен быть `NEXT_PUBLIC_API_BASE_URL`

---

## 📚 Полезные файлы

- `lib/api.js` - API клиент со всеми методами
- `lib/utils/apiTest.js` - функция `testAPI()` для тестирования
- `QUICK_START_ADD_DATA.md` - как быстро добавить данные
- `HOW_TO_ADD_PRODUCTS.md` - подробная инструкция

---

## ✅ Итог

**API работает!** Просто добавьте больше данных на бекенд, и всё заработает полностью.

Для проверки:
1. Выполните `testAPI()` в консоли
2. Проверьте Network tab в DevTools
3. Откройте страницы и посмотрите, что данные загружаются

Удачи! 🚀

